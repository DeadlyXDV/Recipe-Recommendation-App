# Backend API URL
# For development with local server, use your machine's IP address
# Find your IP: 
#   - Linux/Mac: run `hostname -I` or `ifconfig` 
#   - Windows: run `ipconfig`
# Example: API_URL=http://192.168.1.100:5000/api

# For local development (change to your IP)
API_URL=http://192.168.1.100:5000/api

# For production (when server is deployed)
# API_URL=https://your-deployed-server.com/api
